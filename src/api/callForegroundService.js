// نسخة معطّلة مؤقتاً لتشخيص مشكلة إغلاق التطبيق عند الإقلاع —
// لا تستورد notifee إطلاقاً هنا، فقط دوال فارغة (no-op) بنفس التوقيع
// حتى لا نحتاج نغيّر أي ملف آخر يستدعيها.

export async function prewarmCallForegroundService() {
  // معطّل مؤقتاً للتشخيص
}

export async function startCallForegroundService() {
  // معطّل مؤقتاً للتشخيص
}

export async function stopCallForegroundService() {
  // معطّل مؤقتاً للتشخيص
}
